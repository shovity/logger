# master compose

version: '3.7'

services:

  logger:
    image: logger
    build: .
    environment:
      NODE_ENV: production
    depends_on:
      - redis
    networks:
      - backend
      - default
    restart: always

  redis:
    image: redis:alpine
    command: ["redis-server", "--appendonly", "yes"]
    restart: always
    networks:
      - default

networks:
  backend:
    name: backend
